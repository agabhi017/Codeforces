#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, q, k;
    cin >> n >> q >> k;
    vector <int> arr(n);
    for (int i = 0; i < n; i++){
        cin >> arr[i];
    }
    int l, r;
    while (q--){
        cin >> l >> r;

    }
    return 0;
}
